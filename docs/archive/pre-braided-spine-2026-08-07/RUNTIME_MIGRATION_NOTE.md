# Pre-Braided Runtime Migration Note

The active Hearthgate runtime moved to the canonical Braided Spine on 2026-08-07.

Historical runtime semantics remain available through Git lineage at the archive commit recorded by `ARCHIVE_MANIFEST.md`.

Superseded active meanings include:

```text
E = Entropy
M = Momentum / Moonfield
A = Attention / Alignment / Availability
Q = Charge / aggregate / unresolved seventh axis
```

Canonical PREMAQ is:

```text
P = Presence
C = Coherence
R = Resonance
E = Entanglement
M = Memory
A = Agency
Q = Qualia
```

Derived quantities keep their own names and symbols.

The active runtime registry is:

```text
apps/starwell/src/hearthweave-kernel/braided-spine.js
```

The active consolidated authority is:

```text
docs/HEARTHGATE_BRAIDED_SPINE.md
```
