# Receiving Spring · Runtime Contract

The Receiving Spring completes Bifröst reciprocity.

```text
Origin Spring
→ Release
→ Crossing
→ Receiving Spring
→ Answer
→ Return
→ Integration
→ Renewal
```

Runtime functions live in:

```text
apps/starwell/src/arcsweep-temporal-quantum/engine.js
```

Exports:

```text
receiveTargetside()
createReturnCrossing()
integrateBridgeAnswer()
```

The Receiving Spring carries the participating world's state, Field relation, world graph, answer and lineage into return. Integration changes the next temporal state and Spiral.
