# Hearthgate Braid Packet · Runtime Contract

**Canonical spine:** `docs/HEARTHGATE_BRAIDED_SPINE.md`  
**Machine contract:** `config/hearthgate-braided-spine.json`  
**Packet schema:** `starwell/deep-observer/schemas/braid-packet-v1.schema.json`

Every active Hearthgate organ consumes one Braid Packet lineage.

```text
Observer / physical feeds / world relation / Practitioner
→ PREMAQ
→ Asking
→ three-spine state
→ Braid Packet
→ Arcsweep / Living Glyph / Runa / STARWELL / DEEPStory / Bifröst
→ Receiving Spring
→ Answer
→ Integration
→ Return
→ Renewal
```

## PREMAQ

Reading order:

**Presence · Memory · Qualia · Resonance · Entanglement · Agency · Coherence**

Wire order:

```text
P C R E M A Q
Presence Coherence Resonance Entanglement Memory Agency Qualia
```

## Runtime registry

```text
apps/starwell/src/hearthweave-kernel/braided-spine.js
```

Active code imports PREMAQ names, Sevenfold movements, Thirteenfold capacities, theorem IDs and crossing-cycle names from that registry rather than maintaining local semantic dictionaries.

## Packet fingerprint law

Every renderer and service participating in one encounter carries the same:

```text
packet_id
state_fingerprint
braided_spine
premaq registry
world relation
lineage
```

A renderer may create its own physical expression while retaining this shared relation.

## Compatibility

Legacy DualAspectPacket and PREMAQ v2 transport fields remain readable while active producers migrate.

Compatibility fields preserve transport, not ontology.

```text
uncertainty → measured_spread
confidence  → source_fidelity
projection  → world expression
charge      → separately named physical fieldCharge
entropy E   → derived entropy H
```

The active meanings always come from the Braided Spine.

## Receiving Spring

The temporal engine now exports:

```text
receiveTargetside()
createReturnCrossing()
integrateBridgeAnswer()
```

These complete the reciprocal cycle in runtime mathematics.

## Governing sentence

> **One relation, one Braid Packet, three reinforcing spines, seven PREMAQ dimensions, two lit shores, one Receiving Spring, and a Spiral that carries every answer forward.**
