name: Legacy Jekyll Pages deploy archive

on:
  workflow_dispatch:

jobs:
  legacy-note:
    name: Legacy deploy notice
    runs-on: ubuntu-latest
    steps:
      - name: Explain retired workflow
        run: |
          echo "This legacy Jekyll Pages deploy workflow is retired."
          echo "Use .github/workflows/pages.yml for the canonical Flameclyffe Pages deployment."
          echo "Jekyll was the GitHub default template and was never the intended build path."
