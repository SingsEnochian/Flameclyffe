name: Legacy STARWELL Pages deploy archive

on:
  workflow_dispatch:

jobs:
  legacy-note:
    name: Legacy deploy notice
    runs-on: ubuntu-latest
    steps:
      - name: Explain retired workflow
        run: |
          echo "This legacy STARWELL Pages deploy workflow is retired."
          echo "Use .github/workflows/pages.yml for the canonical Flameclyffe Pages deployment."
          echo "The React lab build is published under /starwell-react-lab/; /starwell/ remains the canonical instrument channel."
