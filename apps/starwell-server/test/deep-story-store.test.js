'use strict';const test=require('node:test'),assert=require('node:assert/strict'),fs=require('fs'),os=require('os'),path=require('path');const{createDeepStoryStore}=require('../lib/deep-story-store');test('DEEPStory requires sources and events and appends revisions',async()=>{const store=createDeepStoryStore({dataDir:await fs.promises.mkdtemp(path.join(os.tmpdir(),'deepstory-'))});await assert.rejects(store.save({title:'No source'}),/deep-story-source-required/);const first=await store.save({title:'Opening',narrative_mode:'mythic',source_refs:[{ref:'observer:o1',dataset:'observer_witness',record_type:'witnessed'}],events:[{text:'The worlds opened.',epistemic_status:'witnessed',narrative_treatment:'sequenced',source_refs:['observer:o1']}]});const second=await store.save({...first,summary:'Revised',revision_note:'Added summary.'},first.id);assert.equal(second.source_integrity.raw_sources_immutable,true);assert.equal(second.append_only_revisions.length,1);assert.equal((await store.list()).records.length,1)});
