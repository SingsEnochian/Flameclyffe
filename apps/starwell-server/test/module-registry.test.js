'use strict';const test=require('node:test'),assert=require('node:assert/strict'),fs=require('fs'),os=require('os'),path=require('path');const{createModuleRegistry}=require('../lib/module-registry');test('module registry seeds and permits renaming and disabling without changing stable IDs',async()=>{const dir=await fs.promises.mkdtemp(path.join(os.tmpdir(),'modules-')),seed=path.join(dir,'seed.json');await fs.promises.writeFile(seed,JSON.stringify({schema:'x',modules:[{id:'tones',name:'Tones',route:'/tones',enabled:true,permissions:[]}]}));const r=createModuleRegistry({dataDir:path.join(dir,'data'),seedPath:seed});await r.update('tones',{name:'Resonance Chamber',enabled:false});const m=(await r.list()).modules[0];assert.equal(m.id,'tones');assert.equal(m.name,'Resonance Chamber');assert.equal(m.enabled,false)});
