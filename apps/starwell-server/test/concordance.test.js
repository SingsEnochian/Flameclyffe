'use strict';const test=require('node:test'),assert=require('node:assert/strict');const{searchEntries}=require('../lib/concordance');test('Concordance ranks title and content matches while retaining source identity',()=>{const out=searchEntries([{ref:'a',title:'Sariel',content:'Clarion ascends.',dataset:'Continuity'},{ref:'b',title:'Other',content:'Sariel appears once.',dataset:'Archive'}],'Sariel');assert.equal(out[0].ref,'a');assert.equal(out[1].dataset,'Archive');assert.match(out[1].snippet,/Sariel/) });
