---
skill_type: character
skill_id: kestrelle
version: 1
status: active
world_id: taaveren-vaen
character_id: kestrelle
compiled_from_cells: true
source_bank: ../../cells/characters/kestrelle.cells.json
source_index: ../../source-index.d/kestrelle.json
---

# Kestrelle al’Var

This is the stable entrypoint for Kestrelle’s Arcsweep character cortex. It is not a second character database.

At runtime, compile Kestrelle from the provenance-bearing cell bank plus reviewed local observations relevant to the active world, document, scene, and chronology point.

## Authority

The current Notion character page is the primary active project-canon source. Older Notion records remain available as provenance, but fields explicitly superseded by the newer canonical character page do not activate normally.

Current active name: **Kestrelle al’Var**.

Current era term: **Restoration**.

Older `Kestrelle al’Valar` and `Mending` records remain traceable as superseded source history rather than being silently erased.

## Runtime composition

Resolve:

- identity
- relationships
- boundaries and agency
- character knowledge
- motivation
- current state
- chronology
- supported thinking/perception patterns
- supported dialogue patterns when actual dialogue evidence exists
- reviewed local model observations

Apply both temporal clocks when present:

1. calendar/date validity via `validFrom` / `validUntil`;
2. fictional chronology validity via `storyOrderFrom` / `storyOrderUntil`.

## Knowledge firewall

Kestrelle may use only information available to Kestrelle at the active chronology point. Narrator knowledge, another character’s knowledge, Constellation knowledge, source-canon knowledge, and future canon do not automatically become her knowledge.

A later discovery is activated by its date/story-order gate, not by the model having seen the source.

## Dialogue state

No stable speaking-pattern cells are promoted yet. The current canon sources establish competence, values, perception, history, and internal approach, but do not provide enough dialogue evidence to define a durable cadence without invention.

Dialogue observations may be kept provisionally during writing and promoted later when repeated evidence supports them.

## Learning

Constellation voices may propose `model_observation` cells about Kestrelle from actual prose or scenes. The user chooses whether to keep each observation and explicitly chooses Kestrelle as its target.

These observations remain lower authority than project canon and cannot silently alter her name, history, knowledge, agency, abilities, or chronology.
