import assert from 'node:assert/strict';
import test from 'node:test';
import { createInitialPremaqc, runFeedbackCycle } from '../src/feedback-loop.js';
import { createTransformationRequest } from '../src/transformation-request.js';
import { runRequestedTransformationCircuit } from '../src/requested-transformation-circuit.js';

const world = { id: 'terra-aeterna', name: 'Terra Aeterna', root_hz: 220 };
const baseline = createInitialPremaqc(world.id, { P: .7, C: .62, R: .68, E: .32, M: .7, A: .74, Q: .72 }, '2026-08-14T08:40:00.000Z');
async function makeAsk() { return createTransformationRequest({ world, baselinePremaqc: baseline, description: 'Increase coherence while preserving agency.', targetAxes: ['C', 'R'], direction: 'increase', minimumDelta: .01, intervention: { type: 'writing', strength: .4 }, authority: 'Rowan', consent: true, maximumCycles: 3, stopConditions: ['Feather'], requestedAt: '2026-08-14T08:41:00.000Z' }); }
async function makeFeedback() { return runFeedbackCycle({ world, premaqc: baseline, mode: 'writing', work: 'The bounded Ask is made and then observed.', response: 'A later response is receipted without declaring its cause.', voiceIds: ['lioreal'], observedAt: '2026-08-14T08:45:00.000Z' }); }

test('Requested Transformation closes through Bone, Ash, Intention and topology', async () => { const request = await makeAsk(); const feedbackCycle = await makeFeedback(); const receipt = await runRequestedTransformationCircuit({ request, feedbackCycle, bone: { value: .5, source: 'continuity-gate' }, ashHistory: [{ receipt_id: 'deep-time-previous', source: 'deep-time', state_change: .25, persistence: .9, receipt_confidence: .95, consequence_weight: .8, direction: { C: .03, R: .04 } }], orderParameter: .2 }); assert.equal(receipt.control.cusp_structure_a, -1); assert.equal(receipt.control.cusp_intention_b, .4); assert.equal(receipt.bai.state.bone.value, .5); assert.equal(receipt.bai.state.ash.receipt_count, 1); assert.equal(receipt.bai.state.intention.premaqc_agency, false); assert.ok(['OPEN', 'FOLD_NEAR', 'BRANCH', 'CUSP_NEAR', 'HYSTERETIC'].includes(receipt.bai.topology.state)); assert.equal(receipt.authority.qualia_inferred, false); });
test('prior cusp receipts seed the next Ash trace when explicit Ash is absent', async () => { const request = await makeAsk(); const feedbackCycle = await makeFeedback(); const first = await runRequestedTransformationCircuit({ request, feedbackCycle, bone: .5, orderParameter: -.4 }); const second = await runRequestedTransformationCircuit({ request, feedbackCycle, bone: .5, orderParameter: .4, cuspHistory: [first.cusp.observation_packet] }); assert.equal(second.bai.state.ash.receipt_count, 1); assert.equal(second.bai.state.ash.receipts[0].source, 'arcsweep-cusp-history'); });
