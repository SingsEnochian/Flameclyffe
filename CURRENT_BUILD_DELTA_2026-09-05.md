# FLAMECLYFFE CURRENT BUILD DELTA · 2026-09-05

**Supersedes stale runtime assumptions in the August captured map where they conflict with current `main`.**  
**Current main SHA:** `24051ffccd07ed678582673097a9cfa57914c7de`  
**Primary purpose:** Boxfire local↔web synchronisation and post-Wave-V acceptance.

## Landed since the August operating-map capture

- selected Applet Deck entries now become real desktop/mobile navigation;
- recovered creative and sound organs are first-class applets with explicit launch targets;
- Terra Prime carries additive structured deep-history ingest and all-world completion rules;
- truth/provenance, hydration receipts and world completion diagnostics are live;
- Deep History Atlas, Solar/lunar/geological/human-history views are live;
- cosmology inheritance, uncertainty, multiverse model separation, lineage and divergence views are live;
- Applet completeness matrix, search/filter/favourites and organ health are live;
- external instrument launch preserves active World context;
- House Runtime Braid glyph semantics are shared;
- Glass Halo semantic-capability projection is enforced before model-provider invocation;
- Echo Index uses live adapters over existing stores instead of a duplicate database;
- House Circulation dashboard exists and deliberately refuses synthetic heartbeats.

## Production evidence boundary

At Wave-V implementation time, production contained **0** `model-reply-receipted` rows in `house_runtime_events`.

Therefore:

- first genuine model-reply heartbeat is still an acceptance target;
- full Observer → explicit review → DEEPTime → PREMAQC/Math → feedback circulation remains evidence-gated;
- do not seed rows or convert fixture success into production evidence.

## Current Boxfire task

Read:

`docs/handoffs/BOXFIRE_LOCAL_WEB_SYNC_HANDOFF_2026-09-05.md`

Then synchronise local/native and web modules from the current source contracts, build both from one pinned SHA, run parity tests, and return matching state/receipt identities plus all explicit host-specific divergences.
