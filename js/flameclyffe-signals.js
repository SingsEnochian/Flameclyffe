window.FlameclyffeSignals=(()=>{const DEFAULT_TARGET={label:'Saint Augustine, Florida',latitude:29.9012,longitude:-81.3124};const SCHUMANN=[7.83,14.3,20.8,27.3,33.8];const SCHUMANN_PROXIES=[423,772,1123,1474,1825];const TACTILE_ANCHORS=[27,54,108];const clamp=(v,min,max)=>Math.min(max,Math.max(min,v));const fmt=(v,d=1)=>v==null||Number.isNaN(Number(v))?'n/a':Number(v).toFixed(d);async function fetchJson(url){const res=await fetch(url,{cache:'no-store'});if(!res.ok)throw new Error(url);return res.json()}function latestTableRow(data){if(!Array.isArray(data)||!Array.isArray(data[0]))return null;const headers=data[0].map(x=>String(x).toLowerCase());for(let i=data.length-1;i>0;i--){const row=data[i];if(Array.isArray(row)&&row.some(v=>v!==null&&v!=='')){const out={};headers.forEach((h,j)=>out[h]=row[j]);return out}}return null}function pickNumber(row,parts){if(!row)return null;const key=Object.keys(row).find(k=>parts.every(p=>k.includes(p)));return key?Number(row[key]):null}function solarFlux(data){if(!Array.isArray(data))return null;const rows=data.filter(row=>row&&typeof row==='object');const found=[...rows].reverse().find(row=>String(row.energy||'').includes('0.1-0.8'))||rows.at(-1);return found?Number(found.flux):null}function flareClassFactor(value){const text=String(value||'').toUpperCase();const n=parseFloat(text.slice(1))||1;if(text.startsWith('X'))return clamp(.9+n*.04,0,1.4);if(text.startsWith('M'))return clamp(.65+n*.025,0,1);if(text.startsWith('C'))return clamp(.35+n*.015,0,.72);if(text.startsWith('B'))return .18;if(text.startsWith('A'))return .08;return 0}function fluxFactor(flux){return flux>0?clamp((Math.log10(flux)+8)/4,0,1.25):0}async function fetchSignals(target=DEFAULT_TARGET){const weatherUrl=new URL('https://api.open-meteo.com/v1/forecast');weatherUrl.searchParams.set('latitude',target.latitude);weatherUrl.searchParams.set('longitude',target.longitude);weatherUrl.searchParams.set('timezone','auto');weatherUrl.searchParams.set('current','temperature_2m,relative_humidity_2m,precipitation,cloud_cover,pressure_msl,wind_speed_10m');const base='https://services.swpc.noaa.gov/';const results=await Promise.allSettled([fetchJson(weatherUrl),fetchJson(base+'products/noaa-planetary-k-index.json'),fetchJson(base+'products/solar-wind/plasma-5-minute.json'),fetchJson(base+'products/solar-wind/mag-5-minute.json'),fetchJson(base+'json/goes/primary/xra'+'ys-6-hour.json'),fetchJson(base+'json/goes/primary/xray-flares-latest.json'),fetchJson(base+'json/solar-radio-flux.json')]);const space={};const weather=results[0].status==='fulfilled'?results[0].value.current||null:null;const kpRows=results[1].status==='fulfilled'?results[1].value:null;const kpRow=Array.isArray(kpRows)?kpRows.at(-1):null;if(Array.isArray(kpRow))space.kp=Number(kpRow[1]);const plasma=latestTableRow(results[2].status==='fulfilled'?results[2].value:null);const magnetic=latestTableRow(results[3].status==='fulfilled'?results[3].value:null);space.speed=pickNumber(plasma,['speed']);space.density=pickNumber(plasma,['density']);space.bt=pickNumber(magnetic,['bt']);space.bz=pickNumber(magnetic,['bz']);space.solarFlux=solarFlux(results[4].status==='fulfilled'?results[4].value:null);const flare=results[5].status==='fulfilled'?results[5].value||{}:{};space.flare=flare.classType||flare.class_type||flare.current_class||flare.max_class||'quiet';const radio=results[6].status==='fulfilled'?results[6].value:null;if(Array.isArray(radio)){const last=radio.at(-1);space.f107=Number(last?.flux||last?.observed_flux||last?.[1])||null}return{weather,space}}function modulation(state){const w=state.weather||{},s=state.space||{};const humidity=+(w.relative_humidity_2m??68);const wind=+(w.wind_speed_10m??8);const pressure=+(w.pressure_msl??1013);const cloud=+(w.cloud_cover??35);const precip=+(w.precipitation??0);const kp=+(s.kp??1);const now=new Date();const hour=now.getUTCHours()+now.getUTCMinutes()/60+now.getUTCSeconds()/3600;const timePhase=((hour+state.target.longitude/15+24)%24)/24;const dayOfYear=Math.floor((Date.UTC(now.getUTCFullYear(),now.getUTCMonth(),now.getUTCDate())-Date.UTC(now.getUTCFullYear(),0,0))/86400000);const seasonalPhase=dayOfYear/365.2425;const pressureTilt=clamp((pressure-1013)/45,-1,1);const humidityTilt=clamp((humidity-50)/50,-1,1);const windTilt=clamp(wind/50,0,1);const stormTilt=clamp(precip/8+cloud/180,0,1);const speedFactor=clamp(((s.speed||350)-300)/500,0,1.35);const densityFactor=clamp((s.density||4)/25,0,1.2);const magFactor=clamp((s.bt||5)/30,0,1.2);const southFactor=clamp((-(s.bz||0))/18,0,1.1);const flareFactor=clamp(fluxFactor(s.solarFlux)+flareClassFactor(s.flare)*.35,0,1.4);return{timePhase,seasonalPhase,pressureTilt,humidityTilt,windTilt,stormTilt,kp,kpFactor:clamp(kp/9,0,1),weatherPulse:clamp((humidityTilt+1)*.25+windTilt*.4+stormTilt*.35,0,1),speedFactor,densityFactor,magFactor,southFactor,flareFactor}}function schumannWeight(state,index,mod){const lat=Math.abs(state.target.latitude)*Math.PI/180;return clamp((.55+.45*Math.abs(Math.cos(lat*(index+1))))*(1+mod.humidityTilt*.08+mod.pressureTilt*.06-mod.stormTilt*.05)*(1+mod.kpFactor*.16),.25,1.35)}return{DEFAULT_TARGET,SCHUMANN,SCHUMANN_PROXIES,TACTILE_ANCHORS,fetchSignals,modulation,schumannWeight,fmt,clamp}})();