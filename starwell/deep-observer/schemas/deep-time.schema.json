{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://flameclyffe.local/schemas/deep-time.schema.json",
  "title": "DEEPTime Accepted Temporal Record",
  "description": "Append-only temporal coordinate for human-accepted Arcsweep observations. PREMAQC Q is the binary presence of a receipted firsthand Qualia report; the report itself remains separate context and engineering data quality is separate.",
  "type": "object",
  "additionalProperties": true,
  "required": ["schema_version", "dataset_kind", "id", "world_id", "sequence_id", "sequence_revision", "lambda", "time", "premaqc", "provenance", "quality", "authority"],
  "properties": {
    "schema_version": { "const": "0.1.0" },
    "dataset_kind": { "const": "deep_time" },
    "id": { "type": "string", "minLength": 1 },
    "world_id": { "type": "string", "minLength": 1 },
    "sequence_id": { "type": "string", "minLength": 1 },
    "sequence_revision": { "type": "integer", "minimum": 1 },
    "lambda": { "type": "number" },
    "time": {
      "type": "object",
      "required": ["utc", "julian_date", "julian_time_scale"],
      "properties": {
        "utc": { "type": "string", "format": "date-time" },
        "julian_date": { "type": "number" },
        "julian_time_scale": { "const": "UTC" }
      }
    },
    "premaqc": { "type": "object" },
    "provenance": {
      "type": "object",
      "required": ["observation_run_id", "acceptance_mask_id", "source_receipt_hashes", "accepted_state_hash"],
      "properties": {
        "observation_run_id": { "type": "string" },
        "acceptance_mask_id": { "type": "string" },
        "qualia_report_receipt_id": { "type": ["string", "null"] },
        "source_receipt_hashes": { "type": "array", "minItems": 1, "items": { "type": "string" } },
        "accepted_state_hash": { "type": "string", "minLength": 64, "maxLength": 64 }
      }
    },
    "quality": {
      "type": "object",
      "properties": {
        "data_quality": { "type": ["number", "null"], "minimum": 0, "maximum": 1 },
        "data_quality_axes": { "type": "array", "items": { "enum": ["P", "C", "R", "E", "M", "A"] } },
        "uncertainty": { "type": "object" },
        "qualia_report_present": { "type": "boolean" },
        "missing": { "type": "array" },
        "stale": { "type": "array" }
      },
      "not": { "required": ["Q"] }
    },
    "authority": {
      "type": "object",
      "required": ["append_only", "qualia_presence_is_premaqc_q", "qualia_report_is_firsthand_only", "qualia_magnitude_inference_allowed", "engineering_data_quality_is_q"],
      "properties": {
        "append_only": { "const": true },
        "qualia_presence_is_premaqc_q": { "const": true },
        "qualia_report_is_firsthand_only": { "const": true },
        "qualia_magnitude_inference_allowed": { "const": false },
        "engineering_data_quality_is_q": { "const": false }
      }
    }
  }
}
