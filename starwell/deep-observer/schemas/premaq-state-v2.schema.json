{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://flameclyffe.local/schemas/premaq-state-v2.schema.json",
  "title": "PREMAQ Seven-Dimensional Living Bearing v2",
  "$comment": "Canonical semantics inherit config/hearthgate-braided-spine.json. Reading dimensions: Presence, Memory, Qualia, Resonance, Entanglement, Agency, Coherence. Stable wire order: P,C,R,E,M,A,Q.",
  "type": "object",
  "additionalProperties": false,
  "required": ["schema_version", "id", "observed_at", "registry_version", "state", "receipt_id", "sequence", "model_version"],
  "$defs": {
    "sourceClass": { "enum": ["witnessed", "recorded", "derived", "model-interpreted", "human-interpreted", "remembered", "correlated", "generated", "expressed", "received", "answered", "integrated", "simulated", "projected", "unknown"] },
    "component": {
      "type": "object",
      "additionalProperties": false,
      "required": ["value", "derivative", "uncertainty", "confidence", "contributors"],
      "properties": {
        "value": { "type": "number" },
        "derivative": { "type": "number" },
        "uncertainty": { "type": ["number", "object", "null"], "description": "Legacy v2 transport field. Braided Spine mathematics reads this as measured spread or resolution width." },
        "confidence": { "type": ["number", "null"], "minimum": 0, "maximum": 1, "description": "Legacy v2 transport field. Braided Spine mathematics reads this as source fidelity." },
        "contributors": {
          "type": "array",
          "items": {
            "type": "object",
            "additionalProperties": false,
            "required": ["source_ref", "source_class", "weight", "method"],
            "properties": {
              "source_ref": { "type": "string" },
              "source_class": { "$ref": "#/$defs/sourceClass" },
              "weight": { "type": "number", "minimum": 0, "maximum": 1 },
              "method": { "type": "string" }
            }
          }
        }
      }
    }
  },
  "properties": {
    "schema_version": { "const": "2.0.0" },
    "id": { "type": "string", "minLength": 1 },
    "observed_at": { "type": "string", "format": "date-time" },
    "registry_version": { "type": "string", "minLength": 1 },
    "state": {
      "type": "object",
      "additionalProperties": false,
      "required": ["P", "C", "R", "E", "M", "A", "Q"],
      "properties": {
        "P": { "$ref": "#/$defs/component", "description": "Presence — active here-ness within the encounter" },
        "C": { "$ref": "#/$defs/component", "description": "Coherence — capacity to remain itself through change while sustaining relation" },
        "R": { "$ref": "#/$defs/component", "description": "Resonance — responsive fit, amplification, recurrence, mutual recognition, and answering pattern" },
        "E": { "$ref": "#/$defs/component", "description": "Entanglement — relational coupling across participants, states, worlds, histories, and expressions" },
        "M": { "$ref": "#/$defs/component", "description": "Memory — lineage, recurrence, inheritance, prior crossing, and carried history" },
        "A": { "$ref": "#/$defs/component", "description": "Agency — capacity to initiate, choose, redirect, answer, refuse, create, and transform" },
        "Q": { "$ref": "#/$defs/component", "description": "Qualia — lived interiority and irreducible quality of experience" }
      }
    },
    "receipt_id": { "type": "string", "minLength": 1 },
    "sequence": { "type": "integer", "minimum": 0 },
    "prior_state_ref": { "type": ["string", "null"] },
    "model_version": { "type": "string", "minLength": 1 },
    "provenance_refs": { "type": "array", "items": { "type": "string" }, "default": [] }
  }
}
